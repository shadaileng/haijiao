# 25-帖子收藏功能方案

> **本页信息**
>
> | 项目 | 内容 |
> |------|------|
> | 文档编号 | 25 |
> | 文档版本 | v1.1.0 |
> | 文档状态 | 🏁 已完成 |
> | 最后更新 | 2026-07-29 |
> | 对应功能/内容 | 帖子收藏/取消收藏、收藏状态检查、收藏列表查看、设置页入口 |
>
> **变更历史**
>
> | 日期 | 版本 | 说明 |
> |------|:----:|------|
> | 2026-07-29 | v1.0.0 | 初版 |
> | 2026-07-29 | v1.1.0 | Playwright 调试原站，修正 API 参数 `entityId`，新增 `checkFavorite`，加载时初始化收藏状态 |
>
> **关联文档**：[03-Origin 代码剖析.md](../references/03-Origin%20代码剖析.md)（收藏 API 定义）、[02-功能新增与改善方案.md](./02-功能新增与改善方案.md)

---

## 1. 需求概述

在帖子详情页添加收藏/取消收藏按钮，用户可将感兴趣的帖子加入收藏。收藏列表在设置页新增入口，点击进入独立页面查看已收藏的帖子。

## 2. API 参数（Playwright 调试结论）

通过 Playwright 调试原站 `https://hj2606029d3.top/post/details?pid=2193260`，捕获到原站收藏按钮的实际 API 调用：

| 端点 | 参数 | 说明 |
|------|------|------|
| `GET /api/favorite/add` | `entityId` + `entityType=topic` | 收藏帖子 |
| `GET /api/favorite/delete` | `entityId` + `entityType=topic` | 取消收藏 |
| `GET /api/favorite/favorite` | `entityId` + `entityType=topic` | 检查收藏状态，返回 `{ favorite: boolean }` |

**关键发现**：
- 参数名是 `entityId`（非 `topicId`），使用 `topicId` 会返回"收藏的内容不存在"
- `/favorite/favorite` 在当前后端用于**检查收藏状态**，非收藏列表
- 收藏列表接口暂未找到可用端点，后续需进一步调试

---

## 3. 类型定义

### 修改 `src/types/index.ts`

`Topic` 接口新增字段：

```ts
export interface Topic {
  // ...existing fields
  isFavorite?: boolean  // 是否已收藏
}
```

---

## 4. API 层

### 修改 `src/api/request.ts`

新增 4 个独立导出函数：

```ts
// 收藏帖子
export async function addFavorite(topicId: string | number): Promise<any> {
  return request({ url: '/favorite/add', params: { entityId: topicId, entityType: 'topic' } })
}

// 取消收藏
export async function delFavorite(topicId: string | number): Promise<any> {
  return request({ url: '/favorite/delete', params: { entityId: topicId, entityType: 'topic' } })
}

// 检查帖子是否已收藏
export async function checkFavorite(topicId: string | number): Promise<any> {
  return request({ url: '/favorite/favorite', params: { entityId: topicId, entityType: 'topic' } })
}

// 收藏列表（分页，待后端确认可用端点）
export async function getFavoriteTopics(page: number, limit = 20): Promise<any> {
  return request({ url: '/favorite/favorite', params: { page, limit, entityType: 'topic' } })
}
```

在 `Api` 接口中新增：

```ts
export interface Api {
  // ...existing methods
  addFavorite(params: { params: { topicId: string | number } }): Promise<ApiResult>
  delFavorite(params: { params: { topicId: string | number } }): Promise<ApiResult>
  checkFavorite(params: { params: { topicId: string | number } }): Promise<ApiResult>
  favoriteTopics(params: { params: { page: number; limit?: number } }): Promise<ApiResult>
}
```

在 `api` 对象中注册：

```ts
export const api: Api = {
  // ...existing methods
  async addFavorite({ params }) {
    try {
      const data = await addFavorite(params.topicId)
      return { success: true, data }
    } catch (e: any) {
      return { success: false, message: e.message }
    }
  },
  async delFavorite({ params }) {
    try {
      const data = await delFavorite(params.topicId)
      return { success: true, data }
    } catch (e: any) {
      return { success: false, message: e.message }
    }
  },
  async checkFavorite({ params }) {
    try {
      const data = await checkFavorite(params.topicId)
      return { success: true, data }
    } catch (e: any) {
      return { success: false, message: e.message }
    }
  },
  async favoriteTopics({ params }) {
    try {
      const data = await getFavoriteTopics(params.page, params.limit)
      return { success: true, data }
    } catch (e: any) {
      return { success: false, message: e.message }
    }
  },
}
```

---

## 5. 帖子详情页

### 修改 `src/views/TopicView.vue`

**加载时检查收藏状态**：`loadTopic` 成功后调用 `checkFavorite` 初始化 `isFavorite`。

**操作栏**：在评论数、点赞数旁新增收藏按钮（star/star-o 图标）。

**交互逻辑**：
- 点击调用 `toggleFavorite`，未登录提示"请先登录"
- 已收藏 → 调用 `delFavorite`，取消后 `isFavorite = false`
- 未收藏 → 调用 `addFavorite`，收藏后 `isFavorite = true`
- API 失败时 showToast 提示，UI 不崩溃

**样式**：`.favorite-active { color: #ffc107; }` 已收藏显示黄色。

---

## 6. 收藏列表页

### 新建 `src/views/FavoritesView.vue`

复用 `Topics` 组件，分页加载收藏帖子。

**注意**：当前后端 `/favorite/favorite` 端点用于检查状态而非列表，收藏列表功能待后端确认可用端点后完善。

---

## 7. 路由

### 修改 `src/router/index.ts`

新增路由（需登录）：

```ts
{
  path: '/favorites',
  name: 'Favorites',
  component: () => import('@/views/FavoritesView.vue'),
}
```

---

## 8. 设置页入口

### 修改 `src/views/SettingsView.vue`

在"足迹"和"关注"旁新增"收藏"入口 cell。

---

## 9. E2E 测试

### 新建 `e2e/favorite.spec.ts`

| 测试用例 | 验证内容 |
|---------|---------|
| 帖子详情页显示收藏按钮 | 从热门列表进入帖子，收藏图标可见 |
| 收藏按钮点击切换状态 | 点击后 `isFavorite` 状态切换，验证调用 `checkFavorite` + `addFavorite`/`delete` |
| 设置页显示收藏入口 | `/settings` 页面"收藏"文字可见 |
| 收藏列表页可访问 | `/favorites` 页面"我的收藏"导航栏可见 |

---

## 10. 实施步骤

| 步骤 | 文件 | 操作 | 状态 |
|:----:|:-----|:-----|:----:|
| 1 | `src/types/index.ts` | `Topic` 新增 `isFavorite` 字段 | ✅ |
| 2 | `src/api/request.ts` | 新增 `addFavorite` / `delFavorite` / `checkFavorite` / `getFavoriteTopics` + Api 接口 + api 对象 | ✅ |
| 3 | `src/views/TopicView.vue` | 添加收藏按钮 + 加载时检查状态 + 交互逻辑 + 样式 | ✅ |
| 4 | `src/views/FavoritesView.vue` | 新建收藏列表页 | ✅ |
| 5 | `src/router/index.ts` | 新增 `/favorites` 路由 | ✅ |
| 6 | `src/views/SettingsView.vue` | 添加"收藏"入口 cell | ✅ |
| 7 | `e2e/favorite.spec.ts` | Playwright E2E 测试（4 条用例） | ✅ |
| 8 | — | `pnpm run build` 构建验证 | ✅ |
| 9 | — | `pnpm run test:e2e` E2E 验证（4 passed） | ✅ |
| 10 | `docs/README.md` | 更新文档一览和执行进度 | ✅ |

---

> **文档规范说明**
>
> 1. 文件名格式：`{序号}-{中文标题}.md`
> 2. 版本号格式：`v{major}.{minor}.{patch}`
>    - major：重大内容重构或范围变更
>    - minor：功能增补或章节调整
>    - patch：勘误、格式调整
> 3. 状态标记：
>    - `📋 待执行` — 已规划但未开始
>    - `🚧 进行中` — 正在实施
>    - `🏁 已完成` — 实施完毕
> 4. 新文档按类型放入对应子目录：方案 → `plans/`、架构 → `architecture/`、参考 → `references/`、指南 → `guides/`
> 5. 每更新一次内容，在「变更历史」表格中追加一行
