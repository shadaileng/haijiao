# 主页无限滚动加载修复方案

> **本页信息**
>
> | 项目 | 内容 |
> |------|------|
> | 文档编号 | 16 |
> | 文档版本 | v2.0.0 |
> | 文档状态 | 🏁 已完成 |
> | 最后更新 | 2026-07-15 |
> | 对应功能/内容 | 主页 HotTopicsView 从无限滚动改为分页按钮 |
>
> **变更历史**
>
> | 日期 | 版本 | 说明 |
> |------|:----:|------|
> | 2026-07-15 | v2.0.0 | 改为分页按钮方案，替代 v1 的 nextTick 修复 |
> | 2026-07-15 | v1.0.0 | 初版（nextTick 修复方案，已废弃） |

> **关联文档**：[09-数据驱动重构方案.md](./09-数据驱动重构方案.md)（Comment.vue 已使用 van-pagination 模式）

---

## 1. 问题描述

主页（`/hot`）首次加载正常显示帖子列表，但滚动到底部后不自动加载下一页。v1.0.0 用 `nextTick` 修复了时序问题，但根本的解决方向是改用分页按钮替代无限滚动，提供更可控的浏览体验。

## 2. 设计目标

1. 用 `<van-pagination>` 分页按钮替代 `<van-list>` 无限滚动
2. 翻页后自动滚动到列表顶部
3. 用 `sessionStorage` 记住当前页码（刷新页面不丢失）
4. 仅修改首页（HotTopicsView），不影响其他使用 Topics 组件的页面

## 3. 改动方案

### 3.1 `src/components/Topics.vue`

新增 `mode` prop 支持两种渲染模式：

**新增 Props：**

| Prop | 类型 | 默认值 | 说明 |
|:-----|:----:|:------:|------|
| `mode` | `'scroll' \| 'pagination'` | `'scroll'` | 渲染模式 |
| `pageIndex` | `number` | `1` | 当前页码（pagination 模式） |
| `totalItems` | `number` | `0` | 总条目数（pagination 模式） |
| `pageSize` | `number` | `20` | 每页条数（pagination 模式） |

**新增 Emits：**

| Event | Payload | 说明 |
|:------|:--------|------|
| `pageChange` | `page: number` | 页码切换（pagination 模式） |

**模板变更：**

```html
<!-- scroll 模式：保持现有 van-list，不改变其他视图 -->
<template v-if="mode === 'scroll'">
  <van-list ...>
    ...（现有卡片 v-for）...
  </van-list>
</template>

<!-- pagination 模式：直接渲染列表 + 分页按钮 -->
<template v-else>
  ...（卡片 v-for，van-cell 复用）...
  <div v-if="totalItems > pageSize" class="pagination-wrapper">
    <van-pagination
      :model-value="pageIndex"
      :total-items="totalItems"
      :items-per-page="pageSize"
      @change="(p: number) => emit('pageChange', p)"
    />
  </div>
</template>
```

卡片内容的 `<van-cell v-for="item in topics">` 模板是两种模式共用的，提取到 `<template>` 片段中避免重复。

**暴露的方法：**
- scroll 模式保持 `startLoad` / `endLoad` / `finishLoad`
- pagination 模式暴露空方法，确保 `defineExpose` 接口一致

### 3.2 `src/views/HotTopicsView.vue`

**状态替换：**

```typescript
// 删除
const topicsDom = ref()
const skeletonLoading = ref(true)

// 新增
const pageIndex = ref(Number(sessionStorage.getItem('hotPageIndex')) || 1)
const totalItems = ref(0)
const pageSize = 20
const loading = ref(true)
```

**函数替换：**

```typescript
const loadPage = async (page: number) => {
  loading.value = true
  const result = await api.hot({ params: { page, limit: pageSize } })
  if (!result.success) {
    showToast(result.message || '加载热门列表失败')
    loading.value = false
    return
  }
  topics.length = 0
  topics.push(...result.data.results)
  totalItems.value = result.data.page.total
  pageIndex.value = page
  sessionStorage.setItem('hotPageIndex', String(page))
  loading.value = false
  window.scrollTo({ top: 0, behavior: 'smooth' })
}
```

**onMounted 更新：**

```typescript
onMounted(() => {
  loadPage(pageIndex.value)
})
```

**模板更新：**

```html
<Topics
  mode="pagination"
  :topics="topics"
  :skeletonLoading="loading"
  :pageIndex="pageIndex"
  :totalItems="totalItems"
  :pageSize="pageSize"
  @pageChange="loadPage"
/>
```

### 3.3 `src/types/index.ts`

在 `Page` 接口中补充 `totalPage` 字段：

```typescript
export interface Page {
  index: number
  size: number
  total: number
  totalPage?: number
}
```

该字段由 `toCamelCase()` 从服务端 `total_page` 自动映射而来。

## 4. 边界情况处理

| 场景 | 处理方式 |
|------|----------|
| API 返回空结果 | `van-empty` 由 Topics 组件现有逻辑处理 |
| 总条目 < pageSize（只有一页） | `v-if="totalItems > pageSize"` 隐藏分页按钮 |
| 加载中 | `skeletonLoading` 传递 `true`，Topics 显示骨架屏 |
| 快速翻页 | `loading` 标志防止重复请求 |
| 请求失败 | Toast 提示 + `loading = false` |
| 刷新页面 | `sessionStorage` 恢复上次页码，直接请求对应页 |
| 跨导航返回 | `keep-alive` 缓存保持组件状态，`scrollBehavior` 恢复滚动位置 |

## 5. 不涉及的文件

| 文件 | 原因 |
|:-----|:------|
| `src/views/SearchView.vue` | 仅改首页，保持现有 van-list 不变 |
| `src/views/UserView.vue` | 同上 |
| `src/views/UserHomeView.vue` | 同上 |
| `src/router/index.ts` | scrollBehavior 已有 savedPosition 支持，无需改动 |
| `src/App.vue` | keep-alive 已有 HotTopicsView 缓存，无需改动 |

## 6. 实施步骤

| 步骤 | 任务 | 状态 |
|:---:|------|:----:|
| 1 | 更新方案文档为 v2.0.0（分页方案） | ✅ |
| 2 | `src/components/Topics.vue` 新增 `mode` prop 和 pagination 分支模板 | 📋 |
| 3 | `src/views/HotTopicsView.vue` 重构为分页模式 | 📋 |
| 4 | `src/types/index.ts` 补充 `totalPage` 字段 | 📋 |
| 5 | 更新 `docs/README.md` 文档一览和执行进度 | 📋 |
| 6 | `pnpm run build` 构建验证 | ✅ |
