# 签到与金额显示方案

> **本页信息**
>
> | 项目 | 内容 |
> |------|------|
> | 文档编号 | 28 |
> | 文档版本 | v1.0.0 |
> | 文档状态 | 🏁 已完成 |
> | 最后更新 | 2026-07-31 |
> | 对应功能/内容 | 设置页添加签到按钮和金币/钻石余额显示 |
>
> **变更历史**
>
> | 日期 | 版本 | 说明 |
> |------|:----:|------|
> | 2026-07-31 | v1.0.0 | 初版 |
>
> **关联文档**：[25-帖子收藏功能方案.md](./25-帖子收藏功能方案.md)

---

## 1. 需求概述

在设置页（SettingsView）添加以下功能：
- 显示用户金币和钻石余额
- 提供每日签到按钮

参考原始项目 `haijiao-wxt` 的实现方式。

---

## 2. API 端点

| 方法 | 端点 | 说明 | 参数 |
|------|------|------|------|
| POST | `/user/user_sign_in` | 每日签到 | 无 |
| GET | `/user/wealth` | 获取用户财富信息 | 无 |

### 2.1 签到接口

**请求：**
```http
POST /user/user_sign_in
```

**响应：**
```json
{
  "success": true,
  "data": {
    "gold": 10,
    "连续天数": 5
  }
}
```

### 2.2 用户财富接口

**请求：**
```http
GET /user/wealth
```

**响应：**
```json
{
  "success": true,
  "data": {
    "gold": 1234,
    "diamond": 5678
  }
}
```

**注意：** 钻石值后端以分为单位存储，前端需除以 100 显示。

---

## 3. 类型定义

在 `src/types/index.ts` 中添加：

```typescript
// 签到结果
export interface SignInResult {
  gold: number        // 签到获得的金币
  consecutiveDays?: number  // 连续签到天数
}

// 用户财富
export interface UserWealth {
  gold: number         // 金币余额
  diamond: number      // 钻石余额（后端存储值，需除以100）
}
```

---

## 4. API 层修改

在 `src/api/request.ts` 的 `api` 对象中添加：

```typescript
// 签到
signIn: () => request<SignInResult>({ url: '/user/user_sign_in', method: 'post' }),

// 获取用户财富
wealth: () => request<UserWealth>({ url: '/user/wealth' }),
```

---

## 5. UI 设计

### 5.1 布局结构

```
┌─────────────────────────────────────┐
│  用户信息卡片（已有）                │
│  头像 | 昵称 | ID                   │
│  帖子:0  粉丝:240  关注:98          │
│  签名:...                           │
├─────────────────────────────────────┤
│  💰 我的钱包                        │
│  ┌───────────┬───────────┐          │
│  │ 🪙 金币   │ 💎 钻石    │          │
│  │   1234    │   56.78   │          │
│  └───────────┴───────────┘          │
│  [立即签到]  已连续签到 5 天         │
├─────────────────────────────────────┤
│  登录状态        已登录             │
│  退出登录                         │
│  ...                               │
└─────────────────────────────────────┘
```

### 5.2 交互逻辑

1. **页面加载时**：调用 `/user/wealth` 获取余额并显示
2. **点击签到按钮**：
   - 调用 `/user/user_sign_in`
   - 显示签到结果（获得金币数、连续天数）
   - 刷新余额显示
   - 按钮变为"已签到"状态（禁用）

### 5.3 样式参考

使用 Vant 组件：
- `van-cell-group`：卡片容器
- `van-cell`：列表项
- `van-button`：签到按钮
- `van-tag`：金额标签

---

## 6. 实现步骤

| 步骤 | 文件 | 操作 |
|:----:|------|------|
| 1 | `src/types/index.ts` | 添加 `SignInResult` 和 `UserWealth` 类型 |
| 2 | `src/api/request.ts` | 添加 `signIn` 和 `wealth` API 方法 |
| 3 | `src/views/SettingsView.vue` | 添加钱包 UI 和签到功能 |
| 4 | 测试 | 验证签到和余额显示功能 |

---

## 7. 注意事项

1. **钻石单位转换**：后端钻石以分为单位存储，前端显示需除以 100
2. **签到状态**：需判断今日是否已签到，避免重复签到
3. **未登录处理**：未登录时不显示钱包和签到功能
4. **错误处理**：签到失败时显示 Toast 提示

---

> **文档规范说明**
>
> 1. 文件名格式：`{序号}-{中文标题}.md`
> 2. 版本号格式：`v{major}.{minor}.{patch}`
>    - major：重大内容重构或范围变更
>    - minor：功能增补或章节调整
>    - patch：勘误、格式调整
> 3. 状态标记：
>    - `📋 待执行` — 已规划但未开始
>    - `🚧 进行中` — 正在实施
>    - `🏁 已完成` — 实施完毕
> 4. 新文档按类型放入对应子目录：方案 → `plans/`、架构 → `architecture/`、参考 → `references/`、指南 → `guides/`
> 5. 每更新一次内容，在「变更历史」表格中追加一行
